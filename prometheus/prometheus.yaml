---
global:
  scrape_interval: 15s

# A scrape configuration containing exactly one endpoint to scrape.
scrape_configs:
  - job_name: 'main'
    scrape_interval: 5s
    static_configs:
      - targets: ['main:9090']